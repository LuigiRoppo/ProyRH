{
  "name": "proyrh",
  "version": "1.0.0",
  "scripts": {
    "start:frontend": "npm --prefix control-horario start",
    "build:frontend": "npm --prefix control-horario run build",
    "start:backend": "npm --prefix control-horario-backend start",
    "migrate:backend": "npm --prefix control-horario-backend run migrate"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}