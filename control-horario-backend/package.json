{
  "name": "control-horario-backend",
  "version": "1.0.0",
  "main": "src/app.js",
  "scripts": {
    "start": "npm run migrate && node src/app.js",
    "migrate": "npx knex migrate:latest",
    "vercel-build": "npm install && npm run build",
    "build": "echo 'No build step defined'"
  },
  "dependencies": {
    "@vercel/postgres": "^0.8.0",
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.17.1",
    "knex": "^0.95.0",
    "pg": "^8.12.0",
    "proyrh": "file:..",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "description": ""
}
